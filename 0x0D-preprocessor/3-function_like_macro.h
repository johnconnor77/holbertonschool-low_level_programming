#ifndef _3_function_like_macro_h
#define _3_function_like_macro_h
#define ABS(x) ((x < 0) ? (-x) : (x))
/*check out if x less than 0 so it put negative x
 * besides if it´s not, keep it as x */
#endif
