#include "holberton.h"
/**
 *mul -function that checks if c is digit 0 to 9
 *@a:inpult variable
 *@b:input variable
 *Return: product between a and b
 */
int mul(int a, int b)
{
int val;
return (val = a * b);
}

